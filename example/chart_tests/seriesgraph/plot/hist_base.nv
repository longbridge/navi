// 正确结果：Histogram 基准线在 50，合成值在 50 上下振荡，
// 高于 50 时柱子向上延伸，低于 50 时向下延伸，两侧均能看到
indicator("Chart Test: plot/hist_base", overlay: false);

let val = 50.0 + (close - ta.sma(close, 20));
plot(val, "Value", style: PlotStyle.Histogram, color: Color.BLUE, hist_base: 50.0);
